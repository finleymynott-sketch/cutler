import { Inter } from "next/font/google";

// Modern variable sans-serif font exposed as CSS variable --font-sans
export const sans = Inter({
	subsets: ["latin"],
	variable: "--font-sans",
	display: "swap",
});

export default sans;


